// Test: Show more errors in one pass

Int bar = 5;
Int foo = 6;

// Undefined identifiers
Int blaat = goo + baz * blaat(fool);

// Redeclaration
Int blaat(){
	empty([]);
	return bat + foo + blaat;
}
