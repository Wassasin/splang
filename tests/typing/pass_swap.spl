
(s, t) swap((t, s) x){
	return (snd(x), fst(x));
}

Int test((t, Int) x){
	return fst(swap(x));
}

Void main(){
	(Int, [Bool]) bla = swap(([], 5));
	((Int, Int), Int) baz = swap((1,(2,3)));
	test((1,3));
	test((True, 5));
}
